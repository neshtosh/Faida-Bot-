import Placeholder from "@/components/Placeholder";

export default function SettingsPage() {
  return (
    <Placeholder
      title="Settings"
      icon="⚙️"
      description="Configure bot behaviour, reminder schedules, languages, admin accounts, and connect Supabase for persistence."
      roadmapNote="Admin accounts, reminder times, default language, Supabase connection test panel."
    />
  );
}
